# Root `npm run dev` injects real-backend mode automatically for local integration.
# Keep this file for direct `apps/web` runs or explicit overrides.
NEXT_PUBLIC_USE_MOCK=false
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
DATABASE_URL=sqlite:///./data/homecare-agent.db
